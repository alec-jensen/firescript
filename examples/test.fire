// Fibonacci to n numbers

int n = toInt(input("Enter the number of Fibonacci numbers to generate: ")) + 1;
int a = 0;
int b = 1;

while (n > 0)
{
    print(a);
    int temp = a;
    a = b;
    b = temp + b;
    n = n - 1;
}