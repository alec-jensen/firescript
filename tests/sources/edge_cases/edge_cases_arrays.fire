import @firescript/std.io.println;

// Empty array
empty_arr: int32[] = [];
println(empty_arr.length());

// Single element array
single: int32[] = [42];
println(single[0]);
println(single.length());

// Array with identical elements
same: int32[] = [5, 5, 5, 5];
for (val: int32 in same) {
    println(val);
}

/* EXPECT
0
42
1
5
5
5
5
*/
