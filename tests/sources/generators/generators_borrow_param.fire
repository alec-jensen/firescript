import @firescript/std.io.println;

class Box {
    v: int32;
    fn Box(v: int32) { this.v = v; }
}

fn peekTwice(b: &Box) -> generator<int32> {
    yield b.v;
    yield b.v;
}

box: Box = Box(7);
for (x: int32 in peekTwice(box)) {
    println(x);
}

/* EXPECT
7
7
*/
