import @firescript/std.io.{println};

int32[] a = [1, 2];
int32 cond = 1i32;
if (cond == 1i32) {
    int32[] b = [5];
    println(b.length());
} else {
    int32[] c = [7, 8, 9];
    println(c.length());
}
println(a.length());
