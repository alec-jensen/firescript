// When main() is defined, top-level statements outside it are ignored
// (the compiler logs a warning and only main() runs).
import @firescript/std.io.println;

println("top-level ignored");

fn main() -> void {
    println("only main runs");
}

/* EXPECT
only main runs
*/
