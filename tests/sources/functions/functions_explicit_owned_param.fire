import @firescript/std.io.println;

class Box {
    v: int32;
    fn Box(v: int32) { this.v = v; }
}

fn consume(owned b: Box) -> void {
    println(b.v);
}

b: Box = Box(42);
consume(b);

/* EXPECT
42
*/
