// Test copyable class parsing


import @firescript/std.io.println;

copyable class Vec2 {
    x: float32;
    y: float32;
}

v1: Vec2 = Vec2(1.0f32, 2.0f32);
v2: Vec2 = v1;  // Should copy, not move
println(v1.x);  // Should work - v1 still valid
println(v2.x);

/* EXPECT
1.000000
1.000000
*/
