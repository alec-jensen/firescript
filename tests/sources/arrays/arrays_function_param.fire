import @firescript/std.io.println;

fn sumArray(arr: int32[]) -> int32 {
    sum: int32 = 0;
    for (val: int32 in arr) {
        sum = sum + val;
    }
    return sum;
}

to_sum: int32[] = [1, 2, 3, 4, 5];
total: int32 = sumArray(to_sum);
println(total);  // 15

/* EXPECT
15
*/
