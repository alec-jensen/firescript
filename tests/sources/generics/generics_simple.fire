// Simplest generic function test - just max

import @firescript/std.io.println;

fn max<T>(a: T, b: T) -> T {
    if (a > b) {
        return a;
    }
    return b;
}

x: int32 = max(10, 20);
println(x);

/* EXPECT
20
*/
