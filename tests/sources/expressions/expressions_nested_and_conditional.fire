import @firescript/std.io.println;

int32 result = ((10 + 5) * 2 - 6) / 3;
println(result);  // ((15 * 2) - 6) / 3 = (30 - 6) / 3 = 8

// Conditional value via if/else assignment
int32 max_val = 0;
if (10 > 5) {
    max_val = 10;
} else {
    max_val = 5;
}
println(max_val);

/* EXPECT
8
10
*/
