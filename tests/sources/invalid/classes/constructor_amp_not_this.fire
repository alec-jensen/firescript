// Parser error: '&' receiver marker in a constructor's parameter list not
// followed by the identifier 'this'.

class Foo {
    int32 x;

    Foo(&notThis) {  //~ ERROR FS-PARSE-0010 @10
        this.x = 0;  //~ ERROR FS-PARSE-0003 @9
    }
}
