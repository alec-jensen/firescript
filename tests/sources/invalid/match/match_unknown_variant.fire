// Invalid: the pattern references a variant that doesn't exist on the enum.
// EXPECT: semantic error

enum Shape {
    Circle(radius: float64),
    Point
}

s: Shape = Shape.Point;
match s {  //~ ERROR FS-SEM-0001 @1
    Shape.Circle(radius) -> { }
    Shape.Triangle -> { }  //~ ERROR FS-SEM-0001 @5
}
