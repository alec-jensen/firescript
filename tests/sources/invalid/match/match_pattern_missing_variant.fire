// Parser error: match pattern with an enum type name but no variant name
// after the '.'.

enum Shape {
    Circle(radius: float64),
    Point
}

fn match_pattern_missing_variant(s: Shape) -> void {
    match s {
        Shape. -> 1  //~ ERROR FS-PARSE-0010 @16
        _ -> 0
    }
}
