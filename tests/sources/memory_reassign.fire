// Reassignment semantics test adapted for fixed-size arrays model
// Arrays are fixed-size and non-assignable; test scalar reassignment instead.

import @firescript/std.io.{println};

{
    int32 x = 3;
    println(x);
    x = 42;
    println(x);
}
