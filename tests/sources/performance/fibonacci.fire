//@ snapshot: fir, flir
// Recursive Fibonacci test using functions

import @firescript/std.io.println;

fn fib(n: int64) -> int64 {
    if (n <= 1i64) {
        return n;
    }
    return fib(n - 1i64) + fib(n - 2i64);
}

println(fib(20i64));

/* EXPECT
6765
*/
