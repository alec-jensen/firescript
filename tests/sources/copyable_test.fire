// Test copyable class parsing


import @firescript/std.io.{println};

copyable class Vec2 {
    float32 x;
    float32 y;
}

Vec2 v1 = Vec2(1.0f32, 2.0f32);
Vec2 v2 = v1;  // Should copy, not move
println(v1.x);  // Should work - v1 still valid
println(v2.x);
