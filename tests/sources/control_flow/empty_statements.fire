// Bare/extra semicolons inside a scope are legal empty statements --
// exercises the SEMICOLON skip branch in parse_scope()'s statement loop.
import @firescript/std.io.println;

fn addOne(x: int32) -> int32 {
    ;
    ;
    return x + 1;
    ;
}

println(addOne(4));

/* EXPECT
5
*/
