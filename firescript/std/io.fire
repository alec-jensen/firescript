// firescript std.io library

directive enable_lowlevel_stdout;

export void print<T>(&T value) {
    string text = value as string;
    stdout(text);
}

export void println<T>(&T value) {
    string cast_text = value as string;
    string text = cast_text + "\n";
    drop(cast_text);
    stdout(text);
}