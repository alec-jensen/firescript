"""firescript native backend: self-hosted x86-64 assembler and PE32+ writer.

The compiler emits no C and shells out to no external tools; this package
turns the FLIR backend's assembly into a runnable Windows x64 executable
using only the Python standard library.
"""
